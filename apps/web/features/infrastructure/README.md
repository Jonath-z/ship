The React Flow topology canvas (spec §36) and the node configuration panel.

- `canvas/` — nodes, edges, layout persistence
- `panel/` — the side panel opened by clicking a node
- `config-viewer/` — read-only Monaco pane showing rendered Kamal config, secrets redacted
