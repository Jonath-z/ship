service: acme-production-worker
image: acme-production-worker:latest
servers:
    jobs:
        hosts:
            - 203.0.113.13
ssh:
    user: deploy
    port: 2222
    keys:
        - .kamal/ssh_key
env:
    clear:
        LOG_LEVEL: info
