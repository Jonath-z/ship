// Shared design primitives: buttons, inputs, tables, tabs, modals, toasts,
// status indicators (Healthy / Connected / Failed), empty states, skeletons.
// Status colours are defined once here and used everywhere (SH-124).
export {};
